export class CourseIcon {
    name: string;
    courseCode: string;
    iconName: string;

    constructor(name: string, courseCode: string, iconName: string) {
        this.name = name;
        this.courseCode = courseCode;
        this.iconName = iconName;
    }
}